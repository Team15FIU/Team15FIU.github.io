//Shortens the title of books for less clutter on main page
/*FIXIT: Unable to use in other classes, TypeError: Object(...) is not a function
function shortenTitle(title)
{
    if(title.length > 22)
    {
        return title.substring(0, 22).concat("..."); 
    }
    else
    {
        return title;
    }
}
*/